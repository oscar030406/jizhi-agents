fastapi>=0.115
python-multipart>=0.0.9
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings==2.7.1
langgraph>=1.2
langchain==1.3.1
langchain-deepseek==1.0.1
langchain-openai==1.2.1
numpy>=1.26,<3
requests>=2.32,<3
scikit-learn>=1.5,<2
httpx>=0.28
pytest>=8.2

# PDF 正文抽取（WO-N16 A1）。**2026-08-23 补登记**：这个包漏在这里没写，
# 线上 venv 从来没装过，于是 PDF 抽取在生产一直是缺包降级——本地测得好好的、
# 三本教材判扫描件的测试也绿，唯独没人验证过服务器有没有这个包。
# 教训：加第三方依赖时「本地能跑」和「线上能跑」是两件事，部署要同步装依赖。
pymupdf>=1.28,<2

# scipy 是**运行时依赖**（`backend/rag/retriever.py` 拼稀疏矩阵用），此前只靠
# scikit-learn 把它带进来——线上侥幸装着 1.18.0，但那是间接依赖，
# sklearn 哪天换实现就断了。直接依赖就该直接写。
scipy>=1.11,<2
EduCDM
