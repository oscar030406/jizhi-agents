# ── 内部工作区（对外仓库不含，原地保留） ──────────────────────────────
.claude/
.agents/
CLAUDE.md
AGENTS.md
PLAYBOOK.md
HANDOFF*
*.docx
/docs/*
**/*/docs/
tmp/
dist/
references/
智能制造/
画像/

# ── 数据与产物（体积/隐私/版权） ─────────────────────────────────────
data/
apps/agent-engine/data/
apps/classroom/data/classrooms/*.bak*
*.zip
*.pdf
*.log

# ── 密钥与环境 ───────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.example.*

# ── 构建与依赖 ───────────────────────────────────────────────────────
node_modules/
.next/
# 实验用的 distDir（消融各档、修复验证…）。一条通吃，
# 不要再按名字一个个加——上一次就漏掉了 .next-fixverify。
.next-*/
.venv/
__pycache__/
.pytest_cache/
*.pyc
.turbo/

# ── 备份目录（部署/互换留下的 .bak 家族） ────────────────────────────
*.bak
*.bak-*
.bak-*/
apps/classroom/public/agents/.bak-swap-20260822/

# ── 嵌套仓库自身的元数据（classroom 上游 fork 的 .git 由脚本临时移开） ──
apps/classroom/.git.upstream-bak/

# ── 不属于本项目的文件与上游宣传素材 ─────────────────────────────────
*.pptx
apps/classroom/assets/

# 先行架构研究实验区（赛后论文素材，不公开）
审核架构实验/

# 对外仓库保留的两份文档（放在末尾，压过上面的 /docs/* 与 *.pdf）
!/docs/技术实现文档.pdf
!/docs/部署说明.md
